# Set to "true" to use MSW mock handlers instead of the real backend
VITE_USE_MOCKS=false

# API base URL (default: /api)
VITE_API_BASE_URL=/api

# MSW mock delay in ms (default: 1000)
VITE_MOCK_DELAY=1000
